{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lancer Chrome sur localhost",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}